spring:
  datasource:
    driver-class-name: org.h2.Driver
    username:
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

services:
  rental-delegation:
    url: http://127.0.0.1:8001
